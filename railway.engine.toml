[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.engine"

[deploy]
startCommand = "python engine_api.py"
healthcheckPath = "/api/health"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
